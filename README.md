# devops_netology
test1

.gitignore для Terraform исключает для публикации скрытый локальный каталог .terraform, файлы с расширением tfstate, логи падений.

Кроме того, не публикуются чувствительные данные в файлах с расширением .tfvars.

И файлы переопределения override.tf и подобные. Не переносятся данные при применении конфигурации .terraform.tfstate.lock.info.

Игнорируются файлы конфигурации CLI: 
.terraformrc
terraform.rc

Изменения 12.03
